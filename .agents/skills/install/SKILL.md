---
name: install
description: Use this skill when the user wants to run pip install or installs packages, or uses /install or [INSTALL].
---

# Install Permission

When this skill is active (or the user uses `/install` / `[INSTALL]`), you are permitted to use the `pip install` command.
Without this permission or skill active, you MUST NOT use the `pip install` command.
